import { LandingPage } from '@/domains/landing/components/LandingPage';

/**
 * Home route.
 *
 * This file maps to the "/" URL.
 * The detailed landing page UI lives inside the landing domain.
 */
export default function HomePage() {
  return <LandingPage />;
}
