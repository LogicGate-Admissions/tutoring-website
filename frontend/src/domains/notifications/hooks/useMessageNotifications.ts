'use client';

/**
 * Backwards-compatible export kept for older imports.
 * Prefer useNotifications for new notification types.
 */

export { useNotifications as useMessageNotifications } from './useNotifications';
